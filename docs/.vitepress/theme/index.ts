import DefaultTheme from "vitepress/theme";
import "./custom.css";

// Default VitePress theme as-is for the PR4 scaffold - dark/light mode toggle and local search
// come for free from the default theme. custom.css only overrides the brand accent colors.
// The full homepage hero/feature-card layout is built in Phase 9 PR5, not here.
export default DefaultTheme;
